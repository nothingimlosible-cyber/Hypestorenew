# HYPESTORE Config
# Set these in your Vercel Dashboard (Project Settings > Environment Variables)
IBB=your_imgbb_api_key_here
# Firebase variables are typically in firebase-applet-config.json, 
# but if you use custom ones, add them here.
